import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing",
  description:
    "Explore AuraEDU operating sets for school foundations, growth, intelligence and guided enterprise rollout.",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
