extends: ["spectral:oas"]

# Skeleton contracts are intentionally light; keep path-param mismatches visible
# but do not block generation while the contracts are being refined.
rules:
  path-params: warn
  operation-description: warn
  info-contact: warn
  oas3-unused-component: warn
