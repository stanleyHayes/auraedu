{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.auraedu.dev/events/student.enrolled.v1.json",
  "title": "student.enrolled",
  "description": "Emitted when a student is enrolled in a class.",
  "type": "object",
  "required": [
    "specversion",
    "type",
    "source",
    "id",
    "time",
    "tenant_id",
    "data"
  ],
  "properties": {
    "specversion": {
      "const": "1.0"
    },
    "type": {
      "const": "student.enrolled"
    },
    "source": {
      "const": "student-service"
    },
    "subject": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "time": {
      "type": "string",
      "format": "date-time"
    },
    "tenant_id": {
      "type": "string"
    },
    "datacontenttype": {
      "const": "application/json"
    },
    "data": {
      "type": "object",
      "required": [
        "student_id",
        "class_id",
        "academic_year_id"
      ],
      "properties": {
        "student_id": {
          "type": "string",
          "format": "uuid"
        },
        "class_id": {
          "type": "string",
          "format": "uuid"
        },
        "academic_year_id": {
          "type": "string",
          "format": "uuid"
        },
        "enrollment_date": {
          "type": "string",
          "format": "date"
        }
      }
    }
  }
}
